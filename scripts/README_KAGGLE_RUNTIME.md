# Kaggle runtime utilities

`kaggle_cuda_linker_preflight.py` is the standalone reproduction of the CUDA driver linker repair embedded in FINAL D submission notebooks. Run it only inside a CUDA-enabled Kaggle environment before launching FlashInfer/vLLM.

Successful output must include `CUDA DRIVER LINKER PASS`.
