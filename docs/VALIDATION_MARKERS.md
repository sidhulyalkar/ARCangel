# FINAL D validation markers

A Kaggle Save & Run is eligible for submission only after the log contains the exact FINAL D build ID and reaches: `ARC TOOLKIT PASS`, `VLLM PACKAGE PASS`, `MODEL INPUT PASS`, `CUDA DRIVER RUNTIME LOAD PASS`, `CUDA DRIVER LINKER PASS`, `VLLM SERVER PASS`, `FULL INFRASTRUCTURE PREFLIGHT PASS`, zero-error dynamic public smoke, and `SAVE/RUN VALIDATION PASS`.
