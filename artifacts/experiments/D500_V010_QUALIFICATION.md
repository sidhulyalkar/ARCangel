# D500 V010 Active Scientist qualification

Status: **LOCAL PASS / awaiting Kaggle Save & Run semantic-control receipt**

S170 FINAL D proved the full RTX PRO 6000 + Qwen3.6 27B FP8 + dual-image + official gateway runtime, but its 4-action validation had 3 successful multimodal calls and 4/4 emergency frontier actions with no executable goal hypothesis. V010 targets that semantic-control bridge.

## Passed gates

- canonical single JSON decision contract;
- separate hypothesis/action confidence;
- model-directed exploration distinct from fallback;
- low-confidence rejection occurs before candidate/plan side effects;
- emergency fallback does not contaminate semantic telemetry;
- executable visual-goal ingestion;
- typed hypothesis continuity and contradiction handling;
- mode-specific agency/goal/dynamics/phase/repair fallback;
- ACTION6 repair preserves the exact grounded target;
- optional Python history round trip;
- semantic goal -> exact spatial plan compilation;
- verified queued execution without repeated model calls;
- all source files compile.

## Test receipt

- focused: **23 passed**;
- randomized contract: **20,000 passed**;
- randomized fallback legality: **10,000 passed**;
- randomized hypothesis registry: **5,000 passed**;
- total randomized: **35,000 passed, 0 failed**.

## Kaggle promotion gate

Do not spend the private slot unless Save & Run demonstrates model failures = 0, multiview calls >= 1, model parse successes >= 1, semantic actions >= 1, semantic telemetry distinct from emergency fallback, no silent malformed-contract loop, and a valid official submission parquet.
